import React from 'react';

const Seperator = ({ slice }) => {
    return (
        <section className={`slice__page__features__seperator ${slice.primary.css_classes}`}>
        </section>
    );
};

export default Seperator;
